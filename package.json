{
  "name": "directive-engine-v0.1-impl",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "gen": "tsx src/cli/index.ts --nominal data/toy_nominal_poses.json --asbuilt data/toy_asbuilt_poses.json --constraints data/toy_constraints.json --out out/directives.json",
    "dev": "vite",
    "copy:demo-data": "node scripts/copy-demo-data.mjs",
    "build": "vite build",
    "prebuild": "npm run copy:demo-data",
    "preview": "vite preview",
    "test": "vitest",
    "validate": "node scripts/validate-datasets.mjs",
    "validate:verbose": "node scripts/validate-datasets.mjs --verbose"
  },
  "devDependencies": {
    "@types/qrcode": "^1.5.6",
    "jsdom": "^27.4.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.9"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
